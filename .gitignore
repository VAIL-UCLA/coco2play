__pycache__/
dist/
.DS_Store

# Gaussian splat point clouds are large (~200–800 MB); keep on disk locally.
# Optional: scripts/upload-pointclouds.sh to publish copies via GitHub Releases.
data/scene*/point_cloud.ply

# Pushable assets are not committed (~1k GLBs); host out-of-band when needed.
*.glb

# Local ego observation episodes (viewer.py recorder).
recordings/

models/Coco_GL_SW1k/

deploy/
docs/